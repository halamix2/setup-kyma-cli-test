name: Empty Workflow

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    name: Test on ${{ matrix.os }}
    if: always()
    steps:
      - name: Set up Kyma CLI
        uses: halamix2/setup-kyma-cli-action@0d64ed9b7788518e596cc944564659144ced15e0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Verify Kyma CLI installation
        shell: bash
        run: |
          kyma version
  custom_path:
    runs-on: windows-latest
    steps:
      - name: Set up Kyma CLI
        uses: halamix2/setup-kyma-cli-action@0d64ed9b7788518e596cc944564659144ced15e0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          target-dir: C:\vcpkg
      - name: Verify Kyma CLI installation under cmd
        shell: cmd
        run: |
          kyma version
